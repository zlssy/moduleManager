<!DOCTYPE html>
<html>
<head>
    <title><%= title %></title>
    <link rel='stylesheet' href='/css/style.css' />
    <script src="/lib/require.min.js"></script>
    <script>
        require.config({
            baseUrl: '/modules',
            urlArgs: function () {
                return '?t='+Math.ceil(Date.now()/86400000);
            },
            paths: {
                jquery: '/lib/jquery.min',
                underscore: '/lib/underscore.min'
            },
            shim: {
                underscore: {
                    exports: '_'
                }
            }
        });
        var globalConfig = {
            apiRoot: '/',
            root: '/'
        };
    </script>
</head>
<body>